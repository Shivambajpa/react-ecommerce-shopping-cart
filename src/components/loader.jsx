function Loading() {
  return (
    <div
      style={{
        textAlign: "center",
        padding: "50px",
        fontSize: "22px",
        fontWeight: "bold",
      }}
    >
      Loading Products...
    </div>
  );
}

export default Loading;